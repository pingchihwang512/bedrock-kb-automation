name: Deploy Ritz Tours to Bedrock KB

on:
  push:
    branches: [main]
    paths: ["ritz-tours/*.md"]

permissions:
  id-token: write 
  contents: read 

jobs:
  sync-and-update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install Dependencies
        run: pip install boto3 pyyaml

      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}
          audience: sts.amazonaws.com
          role-skip-session-tagging: true

      - name: Process and Sync
        env:
          S3_BUCKET_NAME: ${{ vars.RITZ_S3_BUCKET_NAME }}
          KNOWLEDGE_BASE_ID: ${{ secrets.RITZ_KNOWLEDGE_BASE_ID }}
          DATA_SOURCE_ID: ${{ secrets.RITZ_DATA_SOURCE_ID }}
          TARGET_FOLDER: "ritz-tours"
        run: python scripts/sync_to_bedrock.py
